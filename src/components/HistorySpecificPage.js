import React from 'react';

/*
    Page for specific writing
*/

const HistorySpecificPage = (props) => (
    <div>
        this is the history page for specific entries
    </div>
);

export default HistorySpecificPage;